build:
  dockerBuildPath: ../
  dockerfile: mcp-pewter-zero/Dockerfile
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    (config) => ({
      command: 'node',
      args: ['dist/server.js'],
      env: {}
    })
